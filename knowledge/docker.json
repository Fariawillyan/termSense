{
  "category": "docker",
  "entries": [
    {
      "name": "docker",
      "kind": "command",
      "summary": "Cria e gerencia containers, imagens, redes e volumes",
      "description": "Imagem é o modelo imutável; container é uma instância em execução dela, isolada por namespaces e cgroups do kernel. No WSL, o Docker Desktop integra o daemon ao Linux; sem ele, instale o docker engine na distribuição.",
      "usage": "docker [OPÇÕES] SUBCOMANDO",
      "install": "Instale o Docker Engine (https://docs.docker.com/engine/install/) ou habilite a integração WSL do Docker Desktop. Para usar sem sudo: sudo usermod -aG docker $USER e relogue.",
      "aliases": ["containers", "container", "docker engine"],
      "tags": ["docker", "containers"],
      "options": [
        { "long": "--context", "arg": "NOME", "description": "Usa outro contexto (daemon remoto)" },
        { "short": "-v", "long": "--version", "description": "Versão" }
      ],
      "examples": [
        { "command": "docker ps", "description": "Containers em execução" },
        { "command": "docker run --rm -it ubuntu bash", "description": "Shell descartável em um container Ubuntu" },
        { "command": "docker compose up -d", "description": "Sobe os serviços do compose.yaml" },
        { "command": "docker info", "description": "Estado do daemon, drivers e quantidade de containers" },
        { "command": "docker system df", "description": "Espaço usado por imagens, containers e volumes" },
        { "command": "docker version", "description": "Versões do cliente e do servidor" }
      ],
      "related": ["docker-ps", "docker-run", "docker-compose", "container", "image"]
    },
    {
      "id": "docker-ps", "name": "docker ps", "kind": "command", "parent": "docker",
      "summary": "Lista containers",
      "usage": "docker ps [OPÇÕES]",
      "aliases": ["listar containers", "containers rodando", "docker container ls"],
      "tags": ["docker", "containers"],
      "options": [
        { "short": "-a", "long": "--all", "description": "Inclui containers parados" },
        { "short": "-q", "long": "--quiet", "description": "Só os IDs" },
        { "short": "-f", "long": "--filter", "arg": "FILTRO", "description": "Filtra (status=exited, name=api)" },
        { "long": "--format", "arg": "MODELO", "description": "Formato Go template, ex.: '{{.Names}} {{.Ports}}'" }
      ],
      "examples": [
        { "command": "docker ps", "description": "Em execução, com portas publicadas" },
        { "command": "docker ps -a", "description": "Inclui parados (veja o STATUS/exit code)" },
        { "command": "docker ps --format 'table {{.Names}}\\t{{.Status}}\\t{{.Ports}}'", "description": "Tabela enxuta" }
      ],
      "related": ["docker-logs", "docker-inspect", "container"]
    },
    {
      "id": "docker-images", "name": "docker images", "kind": "command", "parent": "docker",
      "summary": "Lista as imagens locais",
      "usage": "docker images [OPÇÕES] [REPOSITÓRIO[:TAG]]",
      "aliases": ["listar imagens", "docker image ls"],
      "tags": ["docker", "imagens"],
      "options": [
        { "short": "-a", "long": "--all", "description": "Inclui camadas intermediárias" },
        { "short": "-q", "long": "--quiet", "description": "Só os IDs" },
        { "short": "-f", "long": "--filter", "arg": "FILTRO", "description": "Ex.: dangling=true (sem tag)" }
      ],
      "examples": [
        { "command": "docker images", "description": "Imagens e tamanhos" },
        { "command": "docker image prune", "description": "Remove imagens sem tag" }
      ],
      "related": ["image", "docker-build", "docker-cleanup"]
    },
    {
      "id": "docker-logs", "name": "docker logs", "kind": "command", "parent": "docker",
      "summary": "Mostra a saída (stdout/stderr) de um container",
      "usage": "docker logs [OPÇÕES] CONTAINER",
      "aliases": ["logs do container", "ver logs docker", "container logs"],
      "tags": ["docker", "logs"],
      "options": [
        { "short": "-f", "long": "--follow", "description": "Acompanha em tempo real" },
        { "short": "-n", "long": "--tail", "arg": "N", "description": "Só as últimas N linhas" },
        { "short": "-t", "long": "--timestamps", "description": "Inclui data e hora" },
        { "long": "--since", "arg": "TEMPO", "description": "Desde (10m, 2h, 2024-01-01)" }
      ],
      "arguments": [{ "name": "CONTAINER", "kind": "text", "description": "Nome ou ID do container" }],
      "examples": [
        { "command": "docker logs container", "description": "Toda a saída" },
        { "command": "docker logs -f container", "description": "Acompanha em tempo real" },
        { "command": "docker logs -n 100 -t container", "description": "Últimas 100 linhas com horário" }
      ],
      "related": ["docker-container-logs", "stdout", "stderr", "follow-log"]
    },
    {
      "id": "docker-exec", "name": "docker exec", "kind": "command", "parent": "docker",
      "summary": "Executa um comando dentro de um container em execução",
      "usage": "docker exec [OPÇÕES] CONTAINER COMANDO [ARGUMENTOS]",
      "aliases": ["entrar no container", "shell no container", "executar no container"],
      "tags": ["docker", "containers"],
      "options": [
        { "short": "-i", "long": "--interactive", "description": "Mantém o stdin aberto" },
        { "short": "-t", "long": "--tty", "description": "Aloca um terminal" },
        { "short": "-u", "long": "--user", "arg": "USUÁRIO", "description": "Executa como outro usuário" },
        { "short": "-e", "long": "--env", "arg": "VAR=VALOR", "description": "Define variável de ambiente" },
        { "short": "-w", "long": "--workdir", "arg": "DIR", "description": "Diretório de trabalho" }
      ],
      "arguments": [
        { "name": "CONTAINER", "kind": "text", "description": "Nome ou ID" },
        { "name": "COMANDO", "kind": "command", "description": "O que executar lá dentro", "repeat": true }
      ],
      "examples": [
        { "command": "docker exec -it container sh", "description": "Shell interativo (sh existe em quase toda imagem)" },
        { "command": "docker exec -it container bash", "description": "Bash, se a imagem tiver" },
        { "command": "docker exec container env", "description": "Variáveis de ambiente do container" },
        { "command": "docker exec -u root -it container sh", "description": "Shell como root" }
      ],
      "related": ["docker-enter-container", "container", "docker-ps"]
    },
    {
      "id": "docker-inspect",
      "name": "docker inspect",
      "kind": "command",
      "parent": "docker",
      "summary": "Mostra a configuração completa de containers, imagens, redes ou volumes em JSON",
      "usage": "docker inspect [OPÇÕES] OBJETO...",
      "aliases": ["detalhes do container", "ip do container", "configuração do container"],
      "tags": ["docker"],
      "options": [
        { "short": "-f", "long": "--format", "arg": "MODELO", "description": "Extrai um campo com Go template" },
        { "long": "--type", "arg": "TIPO", "description": "container, image, volume ou network" },
        { "short": "-s", "long": "--size", "description": "Inclui o tamanho (containers)" }
      ],
      "examples": [
        { "command": "docker inspect container", "description": "Tudo em JSON" },
        { "command": "docker inspect -f '{{.State.Status}} {{.State.ExitCode}}' container", "description": "Estado e exit code" },
        { "command": "docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' container", "description": "IP do container" },
        { "command": "docker inspect -f '{{.State.OOMKilled}}' container", "description": "Morreu por falta de memória? (exit 137)" },
        { "command": "docker inspect -f '{{json .Mounts}}' container | jq", "description": "Volumes montados, com jq" },
        { "command": "docker inspect --type image nginx | jq '.[0].Config.Env'", "description": "Variáveis de uma imagem" }
      ],
      "related": ["docker-ps", "docker-network", "docker-container-ip"]
    },
    {
      "id": "docker-network",
      "name": "docker network",
      "kind": "command",
      "parent": "docker",
      "summary": "Gerencia redes de containers",
      "description": "Na rede bridge padrão os containers se falam só por IP; em redes criadas pelo usuário (e no compose) há DNS interno: o nome do serviço resolve para o container.",
      "usage": "docker network ls | inspect | create | connect | rm",
      "aliases": ["rede docker", "redes de containers", "docker network ls"],
      "tags": ["docker", "rede"],
      "examples": [
        { "command": "docker network ls", "description": "Redes existentes" },
        { "command": "docker network create minha-rede", "description": "Rede com DNS interno" },
        { "command": "docker network inspect bridge", "description": "Containers e IPs de uma rede" },
        { "command": "docker network connect minha-rede app", "description": "Liga um container a uma rede" },
        { "command": "docker network rm minha-rede", "description": "Remove a rede" },
        { "command": "docker network prune", "description": "Remove redes sem uso" }
      ],
      "related": ["docker-compose", "dns", "subnet", "nat"]
    },
    {
      "id": "docker-volume",
      "name": "docker volume",
      "kind": "command",
      "parent": "docker",
      "summary": "Gerencia volumes: dados que sobrevivem à remoção do container",
      "usage": "docker volume ls | inspect | create | rm | prune",
      "aliases": ["volumes docker", "persistir dados", "docker volume ls"],
      "tags": ["docker", "armazenamento"],
      "examples": [
        { "command": "docker volume ls", "description": "Volumes existentes" },
        { "command": "docker run -v dados:/var/lib/postgresql/data postgres", "description": "Monta o volume dados" },
        { "command": "docker volume prune", "description": "Remove volumes sem uso (cuidado: apaga dados)" },
        { "command": "docker volume inspect dados", "description": "Onde o volume fica no disco" },
        { "command": "docker volume create dados", "description": "Cria um volume nomeado" },
        { "command": "docker run --rm -v dados:/dados -v $(pwd):/backup alpine tar -czf /backup/dados.tar.gz -C /dados .", "description": "Backup de um volume" }
      ],
      "related": ["container", "docker-compose"]
    },
    {
      "id": "docker-compose",
      "name": "docker compose",
      "kind": "command",
      "parent": "docker",
      "summary": "Sobe e gerencia aplicações de vários containers descritas em compose.yaml",
      "usage": "docker compose [OPÇÕES] up | down | ps | logs | exec | build",
      "aliases": ["docker-compose", "compose", "subir containers", "compose.yaml"],
      "tags": ["docker", "orquestração"],
      "options": [
        { "short": "-f", "long": "--file", "arg": "ARQUIVO", "description": "Arquivo compose" },
        { "short": "-d", "long": "--detach", "description": "Com up: roda em segundo plano" },
        { "long": "--build", "description": "Com up: reconstrói as imagens" },
        { "short": "-v", "long": "--volumes", "description": "Com down: remove também os volumes" }
      ],
      "examples": [
        { "command": "docker compose up -d", "description": "Sobe tudo em segundo plano" },
        { "command": "docker compose ps", "description": "Estado dos serviços" },
        { "command": "docker compose logs -f api", "description": "Logs do serviço api" },
        { "command": "docker compose exec api sh", "description": "Shell no serviço api" },
        { "command": "docker compose down", "description": "Para e remove containers e redes" }
      ],
      "related": ["docker-network", "docker-volume", "docker-logs", "docker-compose-up", "docker-compose-down", "docker-compose-ps"]
    },
    {
      "id": "docker-run", "name": "docker run", "kind": "command", "parent": "docker",
      "summary": "Cria e inicia um container a partir de uma imagem",
      "usage": "docker run [OPÇÕES] IMAGEM [COMANDO]",
      "aliases": ["rodar container", "iniciar container", "criar container"],
      "tags": ["docker", "containers"],
      "options": [
        { "short": "-d", "long": "--detach", "description": "Em segundo plano" },
        { "short": "-it", "description": "Interativo com terminal (-i -t)" },
        { "long": "--rm", "description": "Remove o container ao sair" },
        { "short": "-p", "long": "--publish", "arg": "HOST:CONTAINER", "description": "Publica uma porta (8080:80)" },
        { "short": "-v", "long": "--volume", "arg": "ORIGEM:DESTINO", "description": "Monta volume ou diretório" },
        { "short": "-e", "long": "--env", "arg": "VAR=VALOR", "description": "Variável de ambiente" },
        { "long": "--name", "arg": "NOME", "description": "Nome do container" },
        { "long": "--network", "arg": "REDE", "description": "Rede a usar" }
      ],
      "arguments": [
        { "name": "IMAGEM", "kind": "text", "description": "Imagem (nginx, postgres:16)" },
        { "name": "COMANDO", "kind": "command", "description": "Substitui o comando padrão da imagem", "repeat": true }
      ],
      "examples": [
        { "command": "docker run -d --name web -p 8080:80 nginx", "description": "nginx acessível em localhost:8080" },
        { "command": "docker run --rm -it ubuntu bash", "description": "Shell descartável" },
        { "command": "docker run -d --name db -e POSTGRES_PASSWORD=segredo -v pgdata:/var/lib/postgresql/data -p 5432:5432 postgres:16", "description": "Postgres com volume" }
      ],
      "related": ["container", "image", "porta", "docker-volume"]
    },
    {
      "id": "docker-build", "name": "docker build", "kind": "command", "parent": "docker",
      "summary": "Constrói uma imagem a partir de um Dockerfile",
      "usage": "docker build [OPÇÕES] CONTEXTO",
      "aliases": ["construir imagem", "build image", "dockerfile"],
      "tags": ["docker", "imagens"],
      "options": [
        { "short": "-t", "long": "--tag", "arg": "NOME:TAG", "description": "Nome da imagem" },
        { "short": "-f", "long": "--file", "arg": "ARQUIVO", "description": "Dockerfile alternativo" },
        { "long": "--no-cache", "description": "Ignora o cache de camadas" }
      ],
      "arguments": [{ "name": "CONTEXTO", "kind": "path", "description": "Diretório enviado ao build (normalmente .)" }],
      "examples": [
        { "command": "docker build -t minha-api:1.0 .", "description": "Build com tag" },
        { "command": "docker build --no-cache -t minha-api .", "description": "Sem cache" }
      ],
      "related": ["image", "docker-images"]
    },
    {
      "id": "container",
      "name": "Container",
      "kind": "concept",
      "summary": "Processo isolado com seu próprio sistema de arquivos, rede e limites de recursos",
      "description": "Não é uma VM: compartilha o kernel do host. O isolamento vem de namespaces (PID, rede, mount) e cgroups (CPU, memória). O container vive enquanto seu processo principal (PID 1) estiver rodando.",
      "aliases": ["containers", "contêiner"],
      "tags": ["docker", "containers"],
      "examples": [
        { "command": "docker ps -a", "description": "Containers e estados" },
        { "command": "docker top container", "description": "Processos dentro do container" }
      ],
      "related": ["image", "docker-ps", "process"]
    },
    {
      "id": "image",
      "name": "Imagem Docker",
      "kind": "concept",
      "summary": "Modelo imutável em camadas a partir do qual containers são criados",
      "aliases": ["imagem", "docker image", "camadas"],
      "tags": ["docker", "imagens"],
      "examples": [
        { "command": "docker images", "description": "Imagens locais" },
        { "command": "docker history nginx", "description": "Camadas de uma imagem" }
      ],
      "related": ["container", "docker-build", "docker-images"]
    },
    {
      "id": "docker-enter-container",
      "name": "Entrar em um container",
      "kind": "recipe",
      "summary": "Abrir um shell dentro de um container em execução",
      "aliases": ["entrar no container", "acessar container", "shell no container", "bash no container", "exec container"],
      "tags": ["docker", "containers"],
      "examples": [
        { "command": "docker exec -it container sh", "description": "Shell (sh existe em quase toda imagem)" },
        { "command": "docker exec -it container bash", "description": "Bash, se disponível" },
        { "command": "docker compose exec servico sh", "description": "Em um serviço do compose" }
      ],
      "related": ["docker-exec", "docker-ps"]
    },
    {
      "id": "docker-container-logs",
      "name": "Ver logs de um container",
      "kind": "recipe",
      "summary": "Ler e acompanhar a saída de um container",
      "aliases": ["logs container", "logs do container", "ver logs do container", "container logs"],
      "tags": ["docker", "logs"],
      "examples": [
        { "command": "docker logs container", "description": "Toda a saída" },
        { "command": "docker logs -f container", "description": "Em tempo real" },
        { "command": "docker logs -n 50 container 2>&1 | grep -i error", "description": "Erros nas últimas 50 linhas" }
      ],
      "related": ["docker-logs", "docker-compose"]
    },
    {
      "id": "docker-container-ip",
      "name": "Descobrir o IP de um container",
      "kind": "recipe",
      "summary": "Endereço de um container nas redes Docker",
      "aliases": ["ip do container", "endereço do container", "container ip"],
      "tags": ["docker", "rede"],
      "examples": [
        { "command": "docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' container", "description": "IP em cada rede" },
        { "command": "docker network inspect bridge", "description": "IPs de todos os containers da rede" }
      ],
      "related": ["docker-inspect", "docker-network"]
    },
    {
      "id": "docker-cleanup",
      "name": "Liberar espaço do Docker",
      "kind": "recipe",
      "summary": "Remover containers parados, imagens e caches sem uso",
      "aliases": ["limpar docker", "docker ocupando espaço", "docker prune", "liberar espaço docker"],
      "tags": ["docker", "disco"],
      "examples": [
        { "command": "docker system df", "description": "Quanto espaço cada tipo ocupa" },
        { "command": "docker container prune", "description": "Remove containers parados" },
        { "command": "docker image prune -a", "description": "Remove imagens sem container associado" },
        { "command": "docker system prune", "description": "Containers parados, redes e imagens órfãs" }
      ],
      "related": ["docker-images", "disk-usage", "docker-rm", "docker-rmi"]
    },
    {
      "id": "docker-stop",
      "name": "docker stop",
      "kind": "command",
      "parent": "docker",
      "summary": "Para containers em execução (SIGTERM e, após 10 s, SIGKILL)",
      "usage": "docker stop [-t SEGUNDOS] CONTAINER...",
      "aliases": ["parar container", "desligar container", "stop container"],
      "options": [
        { "short": "-t", "long": "--time", "arg": "SEGUNDOS", "description": "Espera antes do SIGKILL (padrão 10)" }
      ],
      "examples": [
        { "command": "docker stop app", "description": "Para o container app" },
        { "command": "docker stop $(docker ps -q)", "description": "Para todos os containers em execução" },
        { "command": "docker stop -t 30 db", "description": "Dá 30 segundos para o banco fechar" }
      ],
      "related": ["docker-start", "docker-rm", "docker-ps"]
    },
    {
      "id": "docker-start",
      "name": "docker start",
      "kind": "command",
      "parent": "docker",
      "summary": "Inicia containers parados, mantendo a configuração",
      "usage": "docker start [-a] [-i] CONTAINER...",
      "aliases": ["iniciar container", "ligar container", "start container", "docker restart"],
      "options": [
        { "short": "-a", "long": "--attach", "description": "Mostra a saída do container" },
        { "short": "-i", "long": "--interactive", "description": "Liga a entrada (com -a, para containers interativos)" }
      ],
      "examples": [
        { "command": "docker start app", "description": "Inicia de novo" },
        { "command": "docker start -ai meu-ubuntu", "description": "Volta para o shell de um container interativo" },
        { "command": "docker restart app", "description": "Para e inicia" }
      ],
      "related": ["docker-stop", "docker-run", "docker-ps"]
    },
    {
      "id": "docker-rm",
      "name": "docker rm",
      "kind": "command",
      "parent": "docker",
      "summary": "Remove containers parados",
      "usage": "docker rm [-f] [-v] CONTAINER...",
      "aliases": ["remover container", "apagar container", "delete container"],
      "options": [
        { "short": "-f", "long": "--force", "description": "Remove mesmo em execução (mata antes)" },
        { "short": "-v", "long": "--volumes", "description": "Remove também os volumes anônimos" }
      ],
      "examples": [
        { "command": "docker rm app", "description": "Remove um container parado" },
        { "command": "docker rm -f app", "description": "Para e remove" },
        { "command": "docker container prune", "description": "Remove todos os containers parados" }
      ],
      "related": ["docker-rmi", "docker-stop", "docker-cleanup"]
    },
    {
      "id": "docker-rmi",
      "name": "docker rmi",
      "kind": "command",
      "parent": "docker",
      "summary": "Remove imagens locais",
      "usage": "docker rmi [-f] IMAGEM...",
      "aliases": ["remover imagem", "apagar imagem", "delete image"],
      "options": [
        { "short": "-f", "long": "--force", "description": "Remove mesmo com containers parados usando" }
      ],
      "examples": [
        { "command": "docker rmi nginx:1.25", "description": "Remove uma imagem" },
        { "command": "docker image prune", "description": "Remove imagens sem tag (dangling)" },
        { "command": "docker image prune -a", "description": "Remove todas as imagens sem container usando" }
      ],
      "related": ["docker-images", "docker-rm", "docker-cleanup"]
    },
    {
      "id": "docker-cp",
      "name": "docker cp",
      "kind": "command",
      "parent": "docker",
      "summary": "Copia arquivos entre o container e a máquina",
      "usage": "docker cp ORIGEM DESTINO  (container:caminho de um dos lados)",
      "aliases": ["copiar arquivo do container", "copiar para o container", "docker copy"],
      "examples": [
        { "command": "docker cp app:/var/log/app.log .", "description": "Do container para a pasta atual" },
        { "command": "docker cp config.yml app:/etc/app/config.yml", "description": "Da máquina para o container" }
      ],
      "related": ["docker-exec", "docker-volume"]
    },
    {
      "id": "docker-stats",
      "name": "docker stats",
      "kind": "command",
      "parent": "docker",
      "summary": "Uso de CPU, memória, rede e disco de cada container, ao vivo",
      "usage": "docker stats [--no-stream] [CONTAINER...]",
      "aliases": ["uso de memória do container", "cpu do container", "monitorar containers", "docker top"],
      "options": [
        { "long": "--no-stream", "description": "Uma foto em vez de acompanhar" },
        { "short": "-a", "long": "--all", "description": "Inclui containers parados" },
        { "long": "--format", "arg": "MODELO", "description": "Colunas escolhidas com Go template" }
      ],
      "examples": [
        { "command": "docker stats", "description": "Todos os containers, atualizando" },
        { "command": "docker stats --no-stream", "description": "Uma foto" },
        { "command": "docker stats --no-stream --format '{{.Name}} {{.MemUsage}}'", "description": "Só nome e memória" }
      ],
      "related": ["docker-ps", "docker-inspect", "top"]
    },
    {
      "id": "docker-compose-up",
      "name": "docker compose up",
      "kind": "command",
      "parent": "docker-compose",
      "summary": "Cria e inicia os serviços do compose.yaml",
      "usage": "docker compose up [-d] [--build] [SERVIÇO...]",
      "aliases": ["subir serviços", "docker-compose up", "compose up", "subir ambiente"],
      "options": [
        { "short": "-d", "long": "--detach", "description": "Em segundo plano" },
        { "long": "--build", "description": "Reconstrói as imagens antes" },
        { "long": "--force-recreate", "description": "Recria os containers mesmo sem mudança" },
        { "long": "--remove-orphans", "description": "Remove containers de serviços que saíram do arquivo" }
      ],
      "examples": [
        { "command": "docker compose up -d", "description": "Sobe tudo em segundo plano" },
        { "command": "docker compose up -d --build api", "description": "Reconstrói e sobe só a api" },
        { "command": "docker compose up", "description": "Em primeiro plano, com os logs de todos (Ctrl+C para)" }
      ],
      "related": ["docker-compose-down", "docker-compose-logs", "docker-compose-ps"]
    },
    {
      "id": "docker-compose-down",
      "name": "docker compose down",
      "kind": "command",
      "parent": "docker-compose",
      "summary": "Para e remove os containers e redes do compose",
      "usage": "docker compose down [-v] [--rmi all]",
      "aliases": ["derrubar serviços", "docker-compose down", "compose down", "parar ambiente"],
      "options": [
        { "short": "-v", "long": "--volumes", "description": "Remove também os volumes (apaga dados)" },
        { "long": "--rmi", "arg": "TIPO", "description": "Remove imagens: all ou local" }
      ],
      "examples": [
        { "command": "docker compose down", "description": "Para e remove containers e redes (volumes ficam)" },
        { "command": "docker compose down -v", "description": "Zera o ambiente, inclusive os dados" }
      ],
      "related": ["docker-compose-up", "docker-volume"],
      "warnings": ["-v apaga os volumes nomeados do projeto: bancos de dados perdem os dados."]
    },
    {
      "id": "docker-compose-logs",
      "name": "docker compose logs",
      "kind": "command",
      "parent": "docker-compose",
      "summary": "Mostra os logs dos serviços do compose",
      "usage": "docker compose logs [-f] [--tail N] [SERVIÇO...]",
      "aliases": ["logs do compose", "docker-compose logs"],
      "options": [
        { "short": "-f", "long": "--follow", "description": "Acompanha" },
        { "long": "--tail", "arg": "N", "description": "Só as últimas N linhas" },
        { "short": "-t", "long": "--timestamps", "description": "Com data e hora" }
      ],
      "examples": [
        { "command": "docker compose logs -f api", "description": "Acompanha a api" },
        { "command": "docker compose logs --tail 100", "description": "Últimas 100 linhas de cada serviço" }
      ],
      "related": ["docker-logs", "docker-compose-ps"]
    },
    {
      "id": "docker-compose-ps",
      "name": "docker compose ps",
      "kind": "command",
      "parent": "docker-compose",
      "summary": "Lista os containers do projeto compose",
      "usage": "docker compose ps [-a]",
      "aliases": ["serviços rodando", "docker-compose ps", "estado dos serviços"],
      "options": [
        { "short": "-a", "long": "--all", "description": "Inclui os parados" }
      ],
      "examples": [
        { "command": "docker compose ps", "description": "Estado, portas e saúde de cada serviço" },
        { "command": "docker compose ps -a", "description": "Inclui os que saíram (veja o exit code)" }
      ],
      "related": ["docker-ps", "docker-compose-logs"]
    },
    {
      "id": "docker-compose-exec",
      "name": "docker compose exec",
      "kind": "command",
      "parent": "docker-compose",
      "summary": "Executa um comando em um serviço em execução",
      "usage": "docker compose exec SERVIÇO COMANDO",
      "aliases": ["entrar no serviço", "docker-compose exec", "shell no serviço"],
      "options": [
        { "short": "-T", "description": "Sem TTY (para scripts e redirecionamentos)" },
        { "short": "-u", "long": "--user", "arg": "USUÁRIO", "description": "Executa como outro usuário" }
      ],
      "examples": [
        { "command": "docker compose exec api sh", "description": "Shell no serviço api" },
        { "command": "docker compose exec -T db pg_dump -U postgres app > backup.sql", "description": "Backup do banco" }
      ],
      "related": ["docker-exec", "docker-compose-up"]
    },
    {
      "id": "dockerfile",
      "name": "Dockerfile",
      "kind": "concept",
      "summary": "Receita para construir uma imagem: base, arquivos, dependências e comando",
      "description": "Cada instrução cria uma camada em cache: coloque o que muda pouco (dependências) antes do que muda muito (código).",
      "aliases": ["docker file", "criar imagem", "instruções do dockerfile", "from", "cmd", "entrypoint"],
      "tags": ["docker", "imagens"],
      "examples": [
        { "command": "docker build -t minha-app .", "description": "Constrói a imagem a partir do Dockerfile da pasta" },
        { "command": "docker build -f Dockerfile.dev -t minha-app:dev .", "description": "Usa outro arquivo" },
        { "command": "docker history minha-app", "description": "Camadas e tamanhos" }
      ],
      "sections": [
        {
          "title": "Instruções",
          "rows": [
            ["FROM", "imagem base (python:3.12-slim)"],
            ["WORKDIR", "diretório de trabalho dentro da imagem"],
            ["COPY", "copia arquivos do contexto para a imagem"],
            ["RUN", "executa um comando durante o build"],
            ["ENV", "variável de ambiente"],
            ["EXPOSE", "documenta a porta (não publica: isso é o -p)"],
            ["USER", "usuário que roda o processo"],
            ["CMD", "comando padrão (substituível no docker run)"],
            ["ENTRYPOINT", "executável fixo; o CMD vira argumento dele"]
          ]
        },
        { "title": "Exemplo", "text": "FROM python:3.12-slim\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\nUSER nobody\nCMD [\"python\", \"app.py\"]" }
      ],
      "related": ["docker-build", "image", "docker-run"]
    }
  ]
}
